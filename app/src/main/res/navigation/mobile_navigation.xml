<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="com.example.hfund.ChitsFragment"
        android:label="@string/menu_home"
        tools:layout="@layout/content_home">
    </fragment>

    <fragment
        android:id="@+id/nav_balance"
        android:name="com.example.hfund.BalanceFragment"
        android:label="Balance"
        tools:layout="@layout/content_home" />

    <fragment
        android:id="@+id/nav_history"
        android:name="com.example.hfund.HistoryFragment"
        android:label="History"
        tools:layout="@layout/content_home" />
</navigation>